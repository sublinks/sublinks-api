package com.sublinksapp.sublinksappapi.authorization.enums;

public enum AuthorizeAction {
    create,
    read,
    update,
    delete,
    post,
    comment,
    message,
    ban,
    purge,
    follow,
}
